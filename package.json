{
  "name": "tizin-proxy",
  "version": "0.1.0",
  "description": "Test proxy for tizins audio files",
  "main": "proxy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "~4.3.2",
    "request": "~2.36.0"
  }
}
